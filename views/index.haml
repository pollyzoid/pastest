!!!
%title pastest

%h1 pastest

%h2 Recent pastes
%ul
  - @recent.each do |p|
    %li
      %a{:href => "./%s" % p.id}= p.id

%h2 Create new paste

%form{:action => "", :method => "post"}
  %textarea{:name => "body"}
  %input{:type => "submit", :class => "button", :value => "Paste"}
